# -*- mode: snippet -*-
# name: GORM Model Definition
# key: gmodel
# --
package schema

import (
	"time"

	"github.com/google/uuid"
	"gorm.io/gorm"
)

type ${1:User} struct {
	ID        uuid.UUID      `gorm:"type:uuid;primaryKey" json:"id"`
	${2:Name}      ${3:string}         `gorm:"${4:type:varchar(255);not null}" json:"${5:name}"`
	CreatedAt time.Time      `gorm:"autoCreateTime" json:"created_at"`
	UpdatedAt time.Time      `gorm:"autoUpdateTime" json:"updated_at"`
	DeletedAt gorm.DeletedAt `gorm:"index" json:"-"`
}

func (${1:User}) TableName() string {
	return "${6:users}"
}

func (m *${1:User}) BeforeCreate(tx *gorm.DB) error {
	if m.ID == uuid.Nil {
		m.ID = uuid.New()
	}
	return nil
}
$0
